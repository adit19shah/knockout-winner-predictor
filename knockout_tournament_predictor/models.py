from django.db import models
# Create your models here.

class A(models.Model):
    opp_team_name = models.CharField(default="",max_length=50)
    tot_matches = models.IntegerField(default=0)
    no_of_wins = models.IntegerField(default=0)

    def __str__(self):
        return self.opp_team_name

class B(models.Model):
    opp_team_name = models.CharField(default="",max_length=50)
    tot_matches = models.IntegerField(default=0)
    no_of_wins = models.IntegerField(default=0)

    def __str__(self):
        return self.opp_team_name

class C(models.Model):
    opp_team_name = models.CharField(default="",max_length=50)
    tot_matches = models.IntegerField(default=0)
    no_of_wins = models.IntegerField(default=0)

    def __str__(self):
        return self.opp_team_name

class D(models.Model):
    opp_team_name = models.CharField(default="",max_length=50)
    tot_matches = models.IntegerField(default=0)
    no_of_wins = models.IntegerField(default=0)

    def __str__(self):
        return self.opp_team_name

class E(models.Model):
    opp_team_name = models.CharField(default="",max_length=50)
    tot_matches = models.IntegerField(default=0)
    no_of_wins = models.IntegerField(default=0)

    def __str__(self):
        return self.opp_team_name

class F(models.Model):
    opp_team_name = models.CharField(default="",max_length=50)
    tot_matches = models.IntegerField(default=0)
    no_of_wins = models.IntegerField(default=0)

    def __str__(self):
        return self.opp_team_name

class G(models.Model):
    opp_team_name = models.CharField(default="",max_length=50)
    tot_matches = models.IntegerField(default=0)
    no_of_wins = models.IntegerField(default=0)

    def __str__(self):
        return self.opp_team_name

class H(models.Model):
    opp_team_name = models.CharField(default="",max_length=50)
    tot_matches = models.IntegerField(default=0)
    no_of_wins = models.IntegerField(default=0)

    def __str__(self):
        return self.opp_team_name

class I(models.Model):
    opp_team_name = models.CharField(default="",max_length=50)
    tot_matches = models.IntegerField(default=0)
    no_of_wins = models.IntegerField(default=0)

    def __str__(self):
        return self.opp_team_name

class J(models.Model):
    opp_team_name = models.CharField(default="",max_length=50)
    tot_matches = models.IntegerField(default=0)
    no_of_wins = models.IntegerField(default=0)

    def __str__(self):
        return self.opp_team_name

class K(models.Model):
    opp_team_name = models.CharField(default="",max_length=50)
    tot_matches = models.IntegerField(default=0)
    no_of_wins = models.IntegerField(default=0)

    def __str__(self):
        return self.opp_team_name

class L(models.Model):
    opp_team_name = models.CharField(default="",max_length=50)
    tot_matches = models.IntegerField(default=0)
    no_of_wins = models.IntegerField(default=0)

    def __str__(self):
        return self.opp_team_name

class M(models.Model):
    opp_team_name = models.CharField(default="",max_length=50)
    tot_matches = models.IntegerField(default=0)
    no_of_wins = models.IntegerField(default=0)

    def __str__(self):
        return self.opp_team_name

class N(models.Model):
    opp_team_name = models.CharField(default="",max_length=50)
    tot_matches = models.IntegerField(default=0)
    no_of_wins = models.IntegerField(default=0)

    def __str__(self):
        return self.opp_team_name

class O(models.Model):
    opp_team_name = models.CharField(default="",max_length=50)
    tot_matches = models.IntegerField(default=0)
    no_of_wins = models.IntegerField(default=0)

    def __str__(self):
        return self.opp_team_name

class P(models.Model):
    opp_team_name = models.CharField(default="",max_length=50)
    tot_matches = models.IntegerField(default=0)
    no_of_wins = models.IntegerField(default=0)

    def __str__(self):
        return self.opp_team_name
